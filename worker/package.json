{
  "name": "hm-youtube-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker for YouTube API proxy",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
